
import PlaceholderPage from "@/components/PlaceholderPage";
import { Dribbble } from "lucide-react";

const FootballPage = () => {
  return (
    <PlaceholderPage 
      title="Football Betting" 
      description="Bet on football matches from leagues around the world. We offer competitive odds on all major leagues, tournaments, and matches. Join the action now!"
      icon={<Dribbble size={40} />}
    />
  );
};

export default FootballPage;
